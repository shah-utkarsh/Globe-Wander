<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/login_bg"
   >
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:layout_margin="15dp">
    <TextView
        android:id="@+id/tvGlobeWander"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Globe Wander"
        android:textSize="44sp"
        android:textStyle="bold"
        android:textColor="#E9EC2222"
        android:layout_gravity="center" />

    <!-- Email Input centered both horizontally and vertically -->
    <EditText
        android:id="@+id/email"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginBottom="10dp"
        android:hint="@string/email"
        android:padding="10dp"
        android:inputType="textEmailAddress"
        android:background="#94EFEFEF"
        android:layout_gravity="center" />

    <!-- Password Input centered both horizontally and vertically -->
    <EditText
        android:id="@+id/password"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginBottom="5dp"
        android:padding="10dp"
        android:hint="@string/password"
        android:inputType="textPassword"
        android:background="#94EFEFEF"
        android:layout_gravity="center" />

    <!-- Login Button centered both horizontally and vertically -->
    <Button
        android:id="@+id/login"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/login"
        android:backgroundTint="#2196F3"
        android:textColor="#FFFFFF"
        android:layout_gravity="center" />

    <!-- Register Button centered both horizontally and vertically -->
    <Button
        android:id="@+id/register"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/register"
        android:backgroundTint="#FF9800"
        android:layout_gravity="center" />
    </LinearLayout>

</LinearLayout>
